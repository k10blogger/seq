This is an interactive visualisation of symmetrical components, using Processing (and Processing.js).

To run with the docker following command can be used.

```
docker run -d --name seq-app -p 8080:80 -v %cd%:/usr/local/apache2/htdocs/ httpd
```